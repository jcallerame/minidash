{
  "name": "minidash",
  "version": "0.0.3",
  "description": "General-purpose library similar to Lodash, but with smaller footprint.",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "./node_modules/.bin/eslint src/*.js",
    "build": "mkdir -p lib && rm -f lib/* && babel src -d lib"
  },
  "author": "",
  "license": "MIT",
  "sideEffects": false,
  "devDependencies": {
    "@babel/cli": "^7.5.0",
    "@babel/core": "^7.5.0",
    "@babel/preset-env": "^7.5.0",
    "babel-eslint": "^10.0.2",
    "babel-loader": "^8.0.6",
    "eslint": "^6.0.1",
    "uglifyjs-webpack-plugin": "^2.1.3"
  },
  "dependencies": {
    "core-js": "^3.1.4"
  }
}
